# ─────────────────────────────────────────────────────────────────────────────
# Modelos NP3 — dependências Python
# Python 3.10 – 3.13  (recomendado 3.12 para máxima compatibilidade com TF)
#
# Para instalar:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Deep Learning ─────────────────────────────────────────────────────────────
tensorflow>=2.19.0
keras>=3.8.0
keras-tuner>=1.4.7

# ── Ciência de dados ──────────────────────────────────────────────────────────
numpy>=2.1.0
pandas>=2.2.3
scipy>=1.15.0
scikit-learn>=1.6.1

# ── Visualização ──────────────────────────────────────────────────────────────
matplotlib>=3.10.0

# ── Banco de dados (PostgreSQL) ───────────────────────────────────────────────
psycopg2-binary>=2.9.10

# ── Opcional: plotar arquitetura da rede (requer: sudo apt install graphviz)
pydot>=3.0.4

# ── SENTINEL-PIPELINE — Copernicus + geoespacial ──────────────────────────────
# Busca no catálogo STAC do Copernicus Data Space Ecosystem
pystac-client>=0.7.0
# Leitura de bandas raster via /vsicurl/ (sem download de tiles completos)
rasterio>=1.4.0
# Manipulação de geometrias vetoriais
shapely>=2.0.0
# Reprojeção de coordenadas
pyproj>=3.6.0
# Requisições HTTP (autenticação OAuth2 CDSE)
requests>=2.32.0
# Serialização de modelos
joblib>=1.4.0
